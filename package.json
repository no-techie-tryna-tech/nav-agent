{
  "name": "memo-agent",
  "private": true,
  "type": "module",
  "description": "5-agent VC memo pipeline with a deterministic valuation engine",
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
